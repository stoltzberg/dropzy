version: "3.9"

services:
  nginx:
    image: nginx:alpine
    container_name: dropzy-nginx
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./certs:/etc/nginx/certs:ro
      - ./website:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
